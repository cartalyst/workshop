language: php

php:
  - 8.1

cache:
  directories:
    - $HOME/.composer/cache

before_install:
  - composer config -g github-oauth.github.com $CI_USER_TOKEN

install:
  - travis_retry composer install

script:
  - vendor/bin/phpunit
