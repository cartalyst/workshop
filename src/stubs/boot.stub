'boot' => function(ExtensionInterface $extension, Application $app)
	{
		// Get the page model
		$model = $app['{{vendor}}\{{name}}\Models\{{model}}'];

		// Register a new attribute namespace
		$app['Platform\Attributes\Models\Attribute']->registerNamespace($model);
	},
